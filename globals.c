/* =========================================================
   globals.c
   ---------
   This is where the global GameState G actually lives.
   Only one .c file can define G — this is that file.
   All other files just use "extern GameState G" from
   globals.h to access it.
   ========================================================= */

#include "globals.h"

GameState G;


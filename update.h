#ifndef UPDATE_H
#define UPDATE_H

/* =========================================================
   update.h
   --------
   Declaration for update_defense — the main game logic
   function that runs every frame during the defense phase.
   ========================================================= */

#include "globals.h"
#include "utils.h"
#include "spawn.h"

void update_defense(void);

#endif
